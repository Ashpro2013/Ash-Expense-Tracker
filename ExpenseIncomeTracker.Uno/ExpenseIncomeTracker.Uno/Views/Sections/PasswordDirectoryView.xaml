<UserControl x:Class="ExpenseIncomeTracker.Uno.Views.Sections.PasswordDirectoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:ExpenseIncomeTracker.Uno.Models"
             mc:Ignorable="d"
             RequestedTheme="Light">
    <Border Background="{StaticResource PanelBrush}" CornerRadius="18" Padding="18" BorderBrush="#BFDBFE" BorderThickness="1">
        <StackPanel Spacing="14">
            <TextBlock Text="Password Directory" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Store account credentials and related notes." Foreground="{StaticResource TextMutedBrush}" />
            <Border Background="#FFFFFFFF" CornerRadius="12" Padding="12" BorderBrush="#BFDBFE" BorderThickness="1">
                <StackPanel Spacing="10">
                    <TextBox x:Name="PasswordTitleBox" x:FieldModifier="public" Header="Title" />
                    <TextBox x:Name="PasswordUsernameBox" x:FieldModifier="public" Header="Username" />
                    <TextBox x:Name="PasswordValueBox" x:FieldModifier="public" Header="Password" />
                    <TextBox x:Name="PasswordNotesBox" x:FieldModifier="public" Header="Notes" />
                    <Button x:Name="PasswordSaveButton" x:FieldModifier="public" Content="Add Password Entry" Click="OnAddPasswordClicked" />
                </StackPanel>
            </Border>
            <Border Background="#F8FAFC" CornerRadius="12" Padding="8" BorderBrush="#BFDBFE" BorderThickness="1">
                <ListView x:Name="PasswordListView" x:FieldModifier="public" MaxHeight="420">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:PasswordDirectoryEntry">
                            <Border Background="#FFFFFFFF" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind Username}" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{x:Bind MaskedPassword}" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{x:Bind Notes}" Foreground="{StaticResource TextMutedBrush}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Top">
                                        <Button Content="Edit" Tag="{x:Bind Id}" Click="OnEditPasswordClicked" Background="#E0F2FE" Foreground="#075985" BorderBrush="#7DD3FC" />
                                        <Button Content="Delete" Tag="{x:Bind Id}" Click="OnDeletePasswordClicked" Background="#FEE2E2" Foreground="#B91C1C" BorderBrush="#FCA5A5" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
