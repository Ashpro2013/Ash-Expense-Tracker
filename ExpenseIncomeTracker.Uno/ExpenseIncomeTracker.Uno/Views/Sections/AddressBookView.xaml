<UserControl x:Class="ExpenseIncomeTracker.Uno.Views.Sections.AddressBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:ExpenseIncomeTracker.Uno.Models"
             mc:Ignorable="d"
             RequestedTheme="Light">
    <Border Background="{StaticResource PanelBrush}" CornerRadius="18" Padding="18" BorderBrush="#FDE68A" BorderThickness="1">
        <StackPanel Spacing="14">
            <TextBlock Text="Address Book" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Manage contacts with full details." Foreground="{StaticResource TextMutedBrush}" />
            <Border Background="#FFFFFFFF" CornerRadius="12" Padding="12" BorderBrush="#FDE68A" BorderThickness="1">
                <StackPanel Spacing="10">
                    <TextBox x:Name="AddressNameBox" x:FieldModifier="public" Header="Name" />
                    <TextBox x:Name="AddressAddressBox" x:FieldModifier="public" Header="Address" />
                    <TextBox x:Name="AddressPhoneBox" x:FieldModifier="public" Header="Phone no" />
                    <TextBox x:Name="AddressEmailBox" x:FieldModifier="public" Header="Email" />
                    <TextBox x:Name="AddressRemarksBox" x:FieldModifier="public" Header="Remarks" />
                    <Button x:Name="AddressSaveButton" x:FieldModifier="public" Content="Add Address Entry" Click="OnAddAddressClicked" />
                </StackPanel>
            </Border>
            <Border Background="#F8FAFC" CornerRadius="12" Padding="8" BorderBrush="#FDE68A" BorderThickness="1">
                <ListView x:Name="AddressListView" x:FieldModifier="public" MaxHeight="420">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:AddressBookEntry">
                            <Border Background="#FFFFFFFF" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind Address}" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{x:Bind PhoneNo}" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{x:Bind Email}" Foreground="{StaticResource TextMutedBrush}" />
                                        <TextBlock Text="{x:Bind Remarks}" Foreground="{StaticResource TextMutedBrush}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Top">
                                        <Button Content="Edit" Tag="{x:Bind Id}" Click="OnEditAddressClicked" Background="#E0F2FE" Foreground="#075985" BorderBrush="#7DD3FC" />
                                        <Button Content="Delete" Tag="{x:Bind Id}" Click="OnDeleteAddressClicked" Background="#FEE2E2" Foreground="#B91C1C" BorderBrush="#FCA5A5" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
