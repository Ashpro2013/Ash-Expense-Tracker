<UserControl x:Class="ExpenseIncomeTracker.Uno.Views.Sections.PlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:ExpenseIncomeTracker.Uno.Models"
             mc:Ignorable="d"
             RequestedTheme="Light">
    <Border Background="{StaticResource PanelBrush}" CornerRadius="18" Padding="18" BorderBrush="#FED7AA" BorderThickness="1">
        <StackPanel Spacing="14">
            <TextBlock Text="Plan a Day" FontSize="28" FontWeight="SemiBold" />
            <TextBlock Text="Allocate focused time slots and track completion." Foreground="{StaticResource TextMutedBrush}" />
            <Border Background="#FFFFFFFF" CornerRadius="12" Padding="12" BorderBrush="#FED7AA" BorderThickness="1">
                <StackPanel Spacing="10">
                    <DatePicker x:Name="PlanDatePicker" x:FieldModifier="public" Header="Date (yyyy-MM-dd)" />
                    <TextBox x:Name="PlanTitleBox" x:FieldModifier="public" Header="Task title" />
                    <TimePicker x:Name="PlanStartPicker" x:FieldModifier="public" Header="Start time (HH:mm)" ClockIdentifier="24HourClock" />
                    <TimePicker x:Name="PlanEndPicker" x:FieldModifier="public" Header="End time (HH:mm)" ClockIdentifier="24HourClock" />
                    <TextBox x:Name="PlanNotesBox" x:FieldModifier="public" Header="Notes" />
                    <Button x:Name="PlanSaveButton" x:FieldModifier="public" Content="Add Plan Item" Click="OnAddPlanClicked" />
                </StackPanel>
            </Border>
            <Border Background="#F8FAFC" CornerRadius="12" Padding="8" BorderBrush="#FED7AA" BorderThickness="1">
                <ListView x:Name="PlanListView" x:FieldModifier="public" MaxHeight="420">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:DayPlanItem">
                            <Border Background="#FFFFFFFF" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind Notes}" Foreground="{StaticResource TextMutedBrush}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                        <TextBlock Text="{x:Bind StartTime}" VerticalAlignment="Center" />
                                        <TextBlock Text="-" VerticalAlignment="Center" />
                                        <TextBlock Text="{x:Bind EndTime}" VerticalAlignment="Center" Margin="0,0,8,0" />
                                        <Button Content="Edit" Tag="{x:Bind Id}" Click="OnEditPlanClicked" Background="#E0F2FE" Foreground="#075985" BorderBrush="#7DD3FC" />
                                        <Button Content="Done" Tag="{x:Bind Id}" Click="OnTogglePlanClicked" Background="#DCFCE7" Foreground="#166534" BorderBrush="#86EFAC" />
                                        <Button Content="Delete" Tag="{x:Bind Id}" Click="OnDeletePlanClicked" Background="#FEE2E2" Foreground="#B91C1C" BorderBrush="#FCA5A5" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
